- if alert
  #alert
    %p= alert
    %span#close_alert ×
- flash.each do |key, msg|
  - if not key.to_s[/alert/]
    #notice
      %p= msg
      %span#close_notice ×
  - elsif key.to_s.eql?("entry_alert")
    #alert
      %p= msg
      %span#close_alert ×
