#footer
  #devinfo
    #{debug(params) if Rails.env.development?}
  .footerwrapper
    %a.logo{:href => "http://www.intersect.org.au/attribution-policy", :target => "_about"}>
    - if Rails.env.development?
      #dev
        %p HIEv Env: Development. Click for debug info.
    - else
      %p
        Developed by Intersect Australia Ltd. Powered by HIEv Version:
        = render 'shared/tag'
