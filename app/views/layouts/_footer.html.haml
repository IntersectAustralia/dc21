#footer
  #devinfo
    #{debug(params) if Rails.env.development?}
  .footerwrapper
    %a.logo{:href => "http://www.intersect.org.au/attribution-policy", :target => "_about"}>
      %p#intersect_logo intersect_logo
    - if Rails.env.development?
      #dev
        %p DC21 Env: Development. Click for debug info.
    - else
      %p
        Developed by Intersect Australia Ltd. Powered by DC21 Version:
        = render 'shared/tag'
