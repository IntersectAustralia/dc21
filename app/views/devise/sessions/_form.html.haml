.container
  %h2 AAF Login
  - if @aaf_credentials[:email].present?
    .control-group
      .controls
        %small
          Note: You are logged in via AAF as
          %strong #{@aaf_credentials[:email]}
          ,
          - if User.find_by_email(@aaf_credentials[:email])
            but your #{@config.name} account request is pending approval.
          - else
            but you have not registered an account with #{@config.name}.

          %br
          %br
          You may log in with a different account below.
  - else
    .control-group
      .controls
        -#= link_to users_aaf_sign_in_path do
          = image_tag(asset_path("/images/aaf_logo.gif"))
        = link_to "Log in via AAF →", users_aaf_sign_in_path, :class => "btn aaf"

    .control-group
      .controls
        %small
          Note: This will take you to the AAF (Australian Access Federation) login page.
          %br
          %br
          If you are not yet authorised to use #{@config.name} you will be automatically redirected to the #{@config.name} Registration Page after successfully authenticating with your AAF credentials.

.container

.container
  %h2 User Login
  .control-group
    .controls
      %strong Please enter your email and password to log in
  .control-group
    = f.label :email, "Email:", :class => "control-label"
    .controls
      = f.text_field :email
  .control-group
    = f.label :password, "Password:", :class => "control-label"
    .controls
      = f.password_field :password
  .form-actions
    .actions-pre
      = link_to "Sign Up", new_user_registration_path, :class => "btn"
    .actions
      = f.submit "Log in →", :class => ""
      = link_to "Forgot your password?", new_user_password_path, :class => "btn"
