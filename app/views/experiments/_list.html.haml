.form-actions
  .actions-pre
    .experiment.new.whitebutton.btn
      = link_to 'New Experiment', new_facility_experiment_path(@facility)
  %h2 Experiments

- if experiments.empty?
  There are no experiments to display.
- else
  %table#experiments
    %tr
      %th.name Name
      %th.parent Parent
      %th.description Description

    - experiments.each do |experiment|
      %tr
        %td.name= link_to experiment.name , facility_experiment_path(@facility, experiment), :id => "view_#{experiment.id}", :title => 'View'
        %td.parent= experiment.parent_name
        %td.description= experiment.description
