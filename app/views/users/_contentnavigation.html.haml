.contentnavigation
  = tabs_tag(:builder => MenuTabBuilder, :namespace => :contentnavigation) do |tab|
    = tab.users 'Users', users_path
    = tab.accessrequests 'Access Requests', access_requests_users_path
