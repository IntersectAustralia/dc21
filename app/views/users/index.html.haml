- content_for :breadcrumb do
  %h1 Admin / Users

%table.data_table.margin_top_small#users
  %tr.header
    %th.email Email
    %th
      First name
    %th
      Last name
    %th.role Role
    %th.status Status
    %th.date
      Last signed in
    %th.actions

  - @users.each do |user|
    %tr{ :class => cycle('field_bg', 'field_nobg')}
      %td.email= link_to user.email, user, :id => "view_#{user.id}", :title => user.email
      %td{:title => user.first_name}= truncate(user.first_name, :length => 25)
      %td{:title => user.last_name}= truncate(user.last_name, :length => 25)
      %td.role{:title => user.role.name}= user.role.name if user.role
      %td.status= user.status_description
      %td.date
        - if user.last_sign_in_at.nil?
          Never logged in
        - else
          = user.last_sign_in_at.localtime.strftime("%d/%m/%Y %I:%M%p")
      %td.actions
        - if can?(:update_role, User)
          = link_to '', edit_role_user_path(user), :id => "edit_role_#{user.id}", :class => "edit", :title => 'Edit role'
