# Sample localization file for English. Add more files in this directory for other locales.
# See https://github.com/svenfuchs/rails-i18n/tree/master/rails%2Flocale for starting points.

en:
  activerecord:
    attributes:
      data_file:
        file_processing_status: 'File type'
        external_id: 'ID'
        id: 'File ID'
      facility:
        a_lat: 'Latitude'
        a_long: 'Longitude'
        b_lat: 'Latitude (bottom right)'
        b_long: 'Longitude (bottom right)'
      system_configuration:
        auto_ocr_regex: 'Auto OCR Regular Expression:'
        auto_sr_regex: 'Auto Speech Recognition Regular Expression:'
    errors:
      models:
        facility:
          attributes:
            a_lat:
              blank: 'and longitude must both be filled in'
              not_a_number: 'must be a number between -90 and 90'
              inclusion: 'must be a number between -90 and 90'
            a_long:
              blank: 'and latitude must both be filled in'
              not_a_number: 'must be a number between -180 and 180'
              inclusion: 'must be a number between -180 and 180'
            b_lat:
              blank: 'and longitude (bottom right) must both be filled in'
              not_a_number: 'must be a number between -90 and 90'
              inclusion: 'must be a number between -90 and 90'
            b_long:
              blank: 'and latitude (bottom right) must both be filled in'
              not_a_number: 'must be a number between -180 and 180'
              inclusion: 'must be a number between -180 and 180'
